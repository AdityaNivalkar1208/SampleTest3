const user = {
    firstName: 'Johnny',
    address: { houseNumber: '123456' }
}

// ? or . difference 
// user exits nasel tr undefined return karel jr ?. use kela tr
// nusta . lihla aani user exit nasel tr error show karel 

console.log(user.firstName);
console.log(user.address);
console.log(user.address.houseNumber);